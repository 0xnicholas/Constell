# v0.9.0-beta Datasets & Experiments Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Datasets with manual/CSV/trace-derived items, DatasetRun active execution via LLM, Run comparison matrix, and reusable Run Presets. Exit: create dataset → add items → create preset → execute run → compare runs side-by-side.

**Architecture:**

- **Storage:** Prisma adds `Dataset`, `DatasetItem`, `DatasetRun`, `DatasetRunItem`, `DatasetRunPreset`. `DatasetRun.metadata` snapshots all execution params for reproducibility.
- **Worker:** `experimentProcessor` renders prompt variables, calls LLM via extracted shared client, ingests trace/observation via `ingestion-queue`, creates `DatasetRunItem`, then enqueues eval jobs.
- **API:** tRPC routers (`datasets`, `datasetItems`, `datasetRuns`, `datasetRunPresets`) + Public REST under `/api/public/datasets*`.
- **UI:** `/datasets` list, `/datasets/[id]` detail (items + runs tabs), `/datasets/[id]/runs/new` execution form, `/datasets/[id]/compare` side-by-side matrix.
- **SDK:** `client.dataset.*`, `client.datasetItem.*`, `client.datasetRun.*` in TS + Python.

**Tech Stack:** Next.js, tRPC, Prisma, ClickHouse, Redis (ioredis), BullMQ, Tailwind CSS, shadcn/ui, TypeScript SDK, Python SDK

---

## Prerequisites

- `v0.8.0-beta` is on `main`, all tests pass, infra is up (`pnpm run infra:dev:up`).
- Redis, Postgres, ClickHouse containers healthy.

---

## File Structure Map

### Schema changes

```
packages/shared/prisma/schema.prisma                     # MODIFIED: add Dataset, DatasetItem, DatasetRun, DatasetRunItem, DatasetRunPreset
```

### Shared queue contract + LLM client

```
packages/shared/src/server/queues.ts                     # MODIFIED: add datasetRun queue + DatasetRunJob
packages/shared/src/server/llm/client.ts                # NEW: extracted shared LLM client (from evalProcessor)
packages/shared/src/server/index.ts                     # MODIFIED: export LLM client + new queue types
```

### Worker

```
worker/src/services/llmClient.ts                         # MODIFIED: deprecate in favor of shared client (or keep as thin wrapper)
worker/src/queues/experimentProcessor.ts                 # NEW: DatasetRun execution pipeline
worker/src/queues/workerManager.ts                       # MODIFIED: register dataset-run worker
```

### tRPC routers

```
web/src/server/api/routers/datasets.ts                   # NEW: Dataset CRUD
web/src/server/api/routers/datasetItems.ts               # NEW: Item CRUD + createFromTrace + createMany
web/src/server/api/routers/datasetRuns.ts                # NEW: Run CRUD + execute + compare
web/src/server/api/routers/datasetRunPresets.ts          # NEW: Preset CRUD
web/src/server/api/root.ts                               # MODIFIED: register all 4 routers
```

### Public API

```
web/src/pages/api/public/datasets/index.ts               # POST / GET
web/src/pages/api/public/datasets/[name]/index.ts        # GET / DELETE
web/src/pages/api/public/dataset-items/index.ts          # POST / GET
web/src/pages/api/public/dataset-items/[id].ts           # GET / DELETE
web/src/pages/api/public/dataset-runs/index.ts           # POST
web/src/pages/api/public/datasets/[name]/runs/index.ts   # GET
web/src/pages/api/public/datasets/[name]/runs/[runName].ts # GET / DELETE
web/src/pages/api/public/dataset-run-items/index.ts      # POST / GET
```

### UI

```
web/src/features/datasets/components/DatasetTable.tsx    # Dataset list table
web/src/features/datasets/components/DatasetForm.tsx     # Create/edit form
web/src/features/datasets/components/DatasetItemTable.tsx # Item list
web/src/features/datasets/components/DatasetItemForm.tsx # Manual create form
web/src/features/datasets/components/DatasetRunTable.tsx # Run list with status
web/src/features/datasets/components/DatasetRunForm.tsx  # Create run (preset or manual)
web/src/features/datasets/components/DatasetCompareTable.tsx # Side-by-side comparison
web/src/features/datasets/components/DatasetRunPresetTable.tsx # Preset list
web/src/features/datasets/components/DatasetRunPresetForm.tsx  # Preset form
web/src/features/datasets/hooks/useDatasets.ts
web/src/features/datasets/hooks/useDatasetItems.ts
web/src/features/datasets/hooks/useDatasetRuns.ts
web/src/features/datasets/hooks/useDatasetRunPresets.ts

web/src/pages/datasets/index.tsx                         # Dataset list page
web/src/pages/datasets/new.tsx                           # Create dataset
web/src/pages/datasets/[id].tsx                          # Detail (items + runs tabs)
web/src/pages/datasets/[id]/items/new.tsx                # Create item
web/src/pages/datasets/[id]/runs/new.tsx                 # Create run
web/src/pages/datasets/[id]/runs/[runId].tsx             # Run detail
web/src/pages/datasets/[id]/compare.tsx                  # Run comparison
web/src/pages/dataset-run-presets/index.tsx              # Preset list
web/src/pages/dataset-run-presets/new.tsx                # Create preset
web/src/pages/dataset-run-presets/[id].tsx               # Edit preset
```

### SDK additions

```
sdk/typescript/src/datasetClient.ts                      # dataset.*, datasetItem.*, datasetRun.*
sdk/typescript/src/client.ts                             # MODIFIED: wire dataset methods
sdk/python/src/constell/dataset_client.py                # mirror TS surface
sdk/python/src/constell/client.py                        # MODIFIED: wire dataset methods
```

---

## Workflow 1: Schema Migrations

> Must run first. All downstream workflows depend on these schema changes.

### Task 1.1: Prisma — Add Dataset models

**Files:**

- Modify: `packages/shared/prisma/schema.prisma`
- Test: `pnpm --filter @constell/shared run db:generate` passes

- [ ] **Step 1: Add models**

Append at end of `schema.prisma`, after `AnnotationQueueItem` model:

```prisma
// ─── Datasets ───
model Dataset {
  id                   String         @id @default(cuid())
  projectId            String         @map("project_id")
  project              Project        @relation(fields: [projectId], references: [id], onDelete: Cascade)

  name                 String
  description          String?
  inputSchema          Json?          @map("input_schema")
  expectedOutputSchema Json?          @map("expected_output_schema")
  metadata             Json?

  items                DatasetItem[]
  runs                 DatasetRun[]

  createdAt            DateTime       @default(now()) @map("created_at")
  updatedAt            DateTime       @default(now()) @updatedAt @map("updated_at")

  @@unique([projectId, name])
  @@index([projectId])
  @@map("datasets")
}

enum DatasetItemStatus {
  ACTIVE
  ARCHIVED
}

model DatasetItem {
  id                  String            @id @default(cuid())
  projectId           String            @map("project_id")

  status              DatasetItemStatus @default(ACTIVE)
  input               Json?
  expectedOutput      Json?             @map("expected_output")
  metadata            Json?

  sourceTraceId       String?           @map("source_trace_id")
  sourceObservationId String?           @map("source_observation_id")

  datasetId           String            @map("dataset_id")
  dataset             Dataset           @relation(fields: [datasetId, projectId], references: [id, projectId], onDelete: Cascade)

  runItems            DatasetRunItem[]

  createdAt           DateTime          @default(now()) @map("created_at")
  updatedAt           DateTime          @default(now()) @updatedAt @map("updated_at")

  @@index([datasetId])
  @@index([sourceTraceId])
  @@index([sourceObservationId])
  @@map("dataset_items")
}

// ─── Run Presets ───
model DatasetRunPreset {
  id              String   @id @default(cuid())
  projectId       String   @map("project_id")
  project         Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)

  name            String
  description     String?
  promptId        String?  @map("prompt_id")
  promptVersion   Int?     @map("prompt_version")

  model           String
  modelParams     Json?    @map("model_params")
  evalTemplateIds String[] @map("eval_template_ids")

  createdAt       DateTime @default(now()) @map("created_at")
  updatedAt       DateTime @default(now()) @updatedAt @map("updated_at")

  @@unique([projectId, name])
  @@map("dataset_run_presets")
}

// ─── Dataset Runs ───
enum DatasetRunStatus {
  PENDING
  RUNNING
  COMPLETED
  FAILED
  CANCELLED
}

model DatasetRun {
  id              String           @id @default(cuid())
  projectId       String           @map("project_id")

  name            String
  description     String?
  status          DatasetRunStatus @default(PENDING)
  metadata        Json?

  datasetId       String           @map("dataset_id")
  dataset         Dataset          @relation(fields: [datasetId, projectId], references: [id, projectId], onDelete: Cascade)

  runItems        DatasetRunItem[]

  createdAt       DateTime         @default(now()) @map("created_at")
  updatedAt       DateTime         @default(now()) @updatedAt @map("updated_at")

  @@unique([datasetId, projectId, name])
  @@index([datasetId])
  @@index([status])
  @@map("dataset_runs")
}

model DatasetRunItem {
  id            String     @id @default(cuid())
  projectId     String     @map("project_id")

  datasetRunId  String     @map("dataset_run_id")
  datasetRun    DatasetRun @relation(fields: [datasetRunId, projectId], references: [id, projectId], onDelete: Cascade)

  datasetItemId String     @map("dataset_item_id")
  traceId       String     @map("trace_id")
  observationId String?    @map("observation_id")

  createdAt     DateTime   @default(now()) @map("created_at")
  updatedAt     DateTime   @default(now()) @updatedAt @map("updated_at")

  @@index([datasetRunId])
  @@index([datasetItemId])
  @@index([traceId])
  @@map("dataset_run_items")
}
```

Also add `datasets Dataset[]`, `datasetItems DatasetItem[]`, `datasetRuns DatasetRun[]`, `datasetRunPresets DatasetRunPreset[]` to `Project` model relations.

- [ ] **Step 2: Generate migration**

```bash
cd /Users/nicholasl/Documents/build-whatever/Constell
pnpm --filter @constell/shared run db:migrate -- --name add_dataset_models
```

Expected: migration file created under `packages/shared/prisma/migrations/`.

- [ ] **Step 3: Generate Prisma client**

```bash
pnpm --filter @constell/shared run db:generate
```

Expected: `packages/shared/prisma/generated/` updated without errors.

- [ ] **Step 4: Commit**

```bash
git add packages/shared/prisma/
git commit -m "feat(shared): add Dataset, DatasetItem, DatasetRun, DatasetRunItem, DatasetRunPreset models"
```

---

## Workflow 2: Shared Queue Contract + LLM Client Extraction

> Extract reusable LLM client from `evalProcessor`, add `dataset-run-queue` contract.

### Task 2.1: Extract shared LLM client

**Files:**

- Create: `packages/shared/src/server/llm/client.ts`
- Modify: `packages/shared/src/server/index.ts`
- Modify: `worker/src/services/llmClient.ts`
- Modify: `worker/src/queues/evalProcessor.ts`

- [ ] **Step 1: Write shared LLM client**

Create `packages/shared/src/server/llm/client.ts`:

```typescript
export interface LlmCallParams {
  model: string;
  modelParams?: Record<string, unknown>;
  messages: Array<{ role: string; content: string }>;
  apiKey?: string;
}

export interface LlmCallResult {
  content: string;
  usage?: { prompt_tokens: number; completion_tokens: number; total_tokens: number };
  latencyMs: number;
}

export async function callLlm(params: LlmCallParams): Promise<LlmCallResult> {
  const key = params.apiKey ?? process.env.OPENAI_API_KEY;
  if (!key) throw new Error("OPENAI_API_KEY not configured");

  const baseUrl = process.env.OPENAI_BASE_URL ?? "https://api.openai.com/v1";
  const start = Date.now();

  const res = await fetch(`${baseUrl}/chat/completions`, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${key}`,
    },
    body: JSON.stringify({
      model: params.model,
      messages: params.messages,
      ...(params.modelParams ?? {}),
    }),
  });

  if (!res.ok) {
    const err = await res.text();
    throw new Error(`LLM API error ${res.status}: ${err}`);
  }

  const json = (await res.json()) as {
    choices?: Array<{ message?: { content?: string } }>;
    usage?: { prompt_tokens: number; completion_tokens: number; total_tokens: number };
  };

  return {
    content: json.choices?.[0]?.message?.content ?? "",
    usage: json.usage,
    latencyMs: Date.now() - start,
  };
}
```

- [ ] **Step 2: Export from shared barrel**

Modify `packages/shared/src/server/index.ts`, append:

```typescript
export { callLlm } from "./llm/client.js";
export type { LlmCallParams, LlmCallResult } from "./llm/client.js";
```

- [ ] **Step 3: Update worker evalProcessor to use shared client**

Modify `worker/src/queues/evalProcessor.ts`:

Replace import:

```typescript
import { callLlm } from "../services/llmClient.js";
```

With:

```typescript
import { callLlm } from "@constell/shared/src/server";
```

Replace `callLlm(run.template.model, prompt, run.template.temperature ?? 0)` with:

```typescript
await callLlm({
  model: run.template.model,
  messages: [{ role: "user", content: prompt }],
  modelParams: { temperature: run.template.temperature ?? 0 },
});
```

- [ ] **Step 4: Update worker llmClient.ts (thin wrapper or deprecate)**

Modify `worker/src/services/llmClient.ts` to re-export from shared:

```typescript
export { callLlm } from "@constell/shared/src/server";
```

- [ ] **Step 5: Verify typecheck**

```bash
pnpm --filter @constell/shared run typecheck
pnpm --filter worker run typecheck
```

Expected: both pass.

- [ ] **Step 6: Commit**

```bash
git add packages/shared/src/server/llm/ packages/shared/src/server/index.ts worker/src/services/llmClient.ts worker/src/queues/evalProcessor.ts
git commit -m "refactor(shared): extract shared LLM client from evalProcessor"
```

---

### Task 2.2: Add dataset-run-queue contract

**Files:**

- Modify: `packages/shared/src/server/queues.ts`

- [ ] **Step 1: Add queue name and job type**

Modify `packages/shared/src/server/queues.ts`:

```typescript
export const queueNames = {
  ingestion: "ingestion-queue",
  blobStorage: "blobstorage-queue",
  promptCache: "prompt-cache-queue",
  export: "export-queue",
  eval: "eval-queue",
  datasetRun: "dataset-run-queue",
} as const;
```

Add job interface:

```typescript
export interface DatasetRunJob {
  projectId: string;
  datasetId: string;
  datasetRunId: string;
  promptId?: string;
  promptVersion?: number;
  model: string;
  modelParams?: Record<string, unknown>;
  evalTemplateIds: string[];
}
```

Update `QueueJobMap`:

```typescript
export type QueueJobMap = {
  [queueNames.ingestion]: IngestionJob;
  [queueNames.blobStorage]: BlobStorageJob;
  [queueNames.promptCache]: PromptCacheJob;
  [queueNames.export]: ExportJob;
  [queueNames.eval]: EvalJob;
  [queueNames.datasetRun]: DatasetRunJob;
};
```

- [ ] **Step 2: Verify**

```bash
pnpm --filter @constell/shared run typecheck
```

Expected: passes.

- [ ] **Step 3: Commit**

```bash
git add packages/shared/src/server/queues.ts
git commit -m "feat(shared): add dataset-run-queue contract and DatasetRunJob type"
```

---

## Workflow 3: Worker experimentProcessor

> Core execution pipeline: render prompt → LLM call → ingest trace → create RunItem → enqueue evals.

### Task 3.1: Write experimentProcessor

**Files:**

- Create: `worker/src/queues/experimentProcessor.ts`
- Modify: `worker/src/queues/workerManager.ts`
- Modify: `worker/src/app.ts` (if env flag needed)

- [ ] **Step 1: Write processor implementation**

Create `worker/src/queues/experimentProcessor.ts`:

```typescript
import { type Job } from "bullmq";
import { type DatasetRunJob } from "@constell/shared/src/server";
import { prisma } from "@constell/shared/src/db";
import { callLlm } from "@constell/shared/src/server";
import { Queue } from "bullmq";
import Redis from "ioredis";
import { queueNames, type EvalJob } from "@constell/shared/src/server";

const redis = new Redis({
  host: process.env.REDIS_HOST || "localhost",
  port: Number(process.env.REDIS_PORT) || 6379,
  password: process.env.REDIS_AUTH,
  maxRetriesPerRequest: null,
});

const evalQueue = new Queue<EvalJob>(queueNames.eval, { connection: redis });

function renderPromptVariables(template: string, input: unknown): string {
  if (typeof input === "string") {
    return template.replace(/\{\{input\}\}/g, input);
  }
  if (input && typeof input === "object") {
    return template.replace(/\{\{(\w+)\}\}/g, (_match, key) => {
      const val = (input as Record<string, unknown>)[key];
      return val !== undefined ? String(val) : _match;
    });
  }
  return template;
}

async function resolvePromptContent(promptId: string, version?: number | null) {
  const prompt = await prisma.prompt.findUnique({
    where: { id: promptId },
    include: { versions: true },
  });
  if (!prompt) throw new Error(`Prompt ${promptId} not found`);

  let pv = prompt.versions.find((v) => v.version === version);
  if (!pv && version) throw new Error(`Prompt version ${version} not found`);
  if (!pv) pv = prompt.versions.sort((a, b) => b.version - a.version)[0];
  if (!pv) throw new Error(`No versions for prompt ${promptId}`);

  return pv.content;
}

async function ingestTraceViaPipeline(params: {
  projectId: string;
  runId: string;
  datasetItemId: string;
  input: string;
  output: string;
  model: string;
  usage?: { prompt_tokens: number; completion_tokens: number; total_tokens: number };
  latencyMs: number;
}): Promise<{ traceId: string; observationId: string }> {
  // For v0.9.0, directly write to Prisma to avoid circular dependency on ingestion-queue
  // Future: enqueue to ingestion-queue for consistent enrichment
  const traceId = `trace-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`;
  const observationId = `obs-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`;

  await prisma.trace.create({
    data: {
      id: traceId,
      projectId: params.projectId,
      name: `dataset-run-${params.runId}`,
      metadata: { datasetRunId: params.runId, datasetItemId: params.datasetItemId },
      latencyMs: Math.round(params.latencyMs),
      totalTokens: params.usage?.total_tokens ?? 0,
    },
  });

  await prisma.observation.create({
    data: {
      id: observationId,
      traceId,
      projectId: params.projectId,
      type: "GENERATION",
      input: params.input,
      output: params.output,
      model: params.model,
      inputTokens: params.usage?.prompt_tokens ?? 0,
      outputTokens: params.usage?.completion_tokens ?? 0,
      totalTokens: params.usage?.total_tokens ?? 0,
    },
  });

  return { traceId, observationId };
}

export async function processExperimentJob(job: Job<DatasetRunJob>): Promise<void> {
  const { projectId, datasetId, datasetRunId, ...execParams } = job.data;

  await prisma.datasetRun.update({
    where: { id: datasetRunId },
    data: { status: "RUNNING" },
  });

  const dataset = await prisma.dataset.findUnique({
    where: { id: datasetId },
    include: { items: { where: { status: "ACTIVE" } } },
  });

  if (!dataset || dataset.items.length === 0) {
    await prisma.datasetRun.update({
      where: { id: datasetRunId },
      data: { status: "FAILED", metadata: { ...execParams, error: "No active items in dataset" } },
    });
    return;
  }

  let promptContent: string | null = null;
  if (execParams.promptId) {
    try {
      promptContent = await resolvePromptContent(execParams.promptId, execParams.promptVersion);
    } catch (err) {
      await prisma.datasetRun.update({
        where: { id: datasetRunId },
        data: {
          status: "FAILED",
          metadata: { ...execParams, error: (err as Error).message },
        },
      });
      return;
    }
  }

  const failures: Array<{ itemId: string; reason: string }> = [];

  for (const item of dataset.items) {
    try {
      const rendered = promptContent
        ? renderPromptVariables(promptContent, item.input)
        : JSON.stringify(item.input);

      const llmRes = await callLlm({
        model: execParams.model,
        messages: [{ role: "user", content: rendered }],
        modelParams: execParams.modelParams,
      });

      const { traceId, observationId } = await ingestTraceViaPipeline({
        projectId,
        runId: datasetRunId,
        datasetItemId: item.id,
        input: rendered,
        output: llmRes.content,
        model: execParams.model,
        usage: llmRes.usage,
        latencyMs: llmRes.latencyMs,
      });

      await prisma.datasetRunItem.create({
        data: {
          projectId,
          datasetRunId,
          datasetItemId: item.id,
          traceId,
          observationId,
        },
      });

      for (const templateId of execParams.evalTemplateIds) {
        await evalQueue.add("eval-run-item", {
          projectId,
          templateId,
          runId: "", // Not an evalRun, just a single trace eval
          traceId,
          observationId,
        } as EvalJob);
      }
    } catch (err) {
      failures.push({ itemId: item.id, reason: (err as Error).message });
    }
  }

  const allFailed = failures.length === dataset.items.length;
  await prisma.datasetRun.update({
    where: { id: datasetRunId },
    data: {
      status: allFailed ? "FAILED" : "COMPLETED",
      metadata: {
        ...execParams,
        processedCount: dataset.items.length - failures.length,
        failureCount: failures.length,
        failures: failures.slice(0, 50),
      },
    },
  });
}
```

- [ ] **Step 2: Register worker**

Modify `worker/src/queues/workerManager.ts`:

Add import:

```typescript
import { processExperimentJob } from "./experimentProcessor.js";
```

After evalWorker registration, add:

```typescript
const datasetRunWorker = new Worker<QueueJobMap[(typeof queueNames)["datasetRun"]]>(
  queueNames.datasetRun,
  async (job) => {
    console.log(`[dataset-run] Processing job ${job.id}`, job.data.datasetRunId);
    return processExperimentJob(job);
  },
  { connection: redis, concurrency: 1 }
);
workers.push(datasetRunWorker);
```

- [ ] **Step 3: Typecheck**

```bash
pnpm --filter worker run typecheck
```

Expected: passes.

- [ ] **Step 4: Commit**

```bash
git add worker/src/queues/experimentProcessor.ts worker/src/queues/workerManager.ts
git commit -m "feat(worker): add experimentProcessor for dataset-run-queue"
```

---

## Workflow 4: tRPC Routers

> Register 4 new routers, wire into root. Implement CRUD + execution logic.

### Task 4.1: datasets router

**Files:**

- Create: `web/src/server/api/routers/datasets.ts`

- [ ] **Step 1: Write router**

Create `web/src/server/api/routers/datasets.ts`:

```typescript
import { z } from "zod";
import { TRPCError } from "@trpc/server";
import { authedProcedure, createTRPCRouter } from "../trpc";
import { prisma } from "@constell/shared/src/db";

export const datasetsRouter = createTRPCRouter({
  list: authedProcedure
    .input(
      z.object({
        projectId: z.string().optional(),
        search: z.string().optional(),
        page: z.number().default(0),
        limit: z.number().default(20),
      })
    )
    .query(async ({ input, ctx }) => {
      const projectId = ctx.projectId ?? input.projectId;
      if (!projectId) throw new TRPCError({ code: "BAD_REQUEST", message: "projectId required" });

      const where = {
        projectId,
        name: input.search ? { contains: input.search, mode: "insensitive" as const } : undefined,
      };
      const [data, totalCount] = await Promise.all([
        prisma.dataset.findMany({
          where,
          skip: input.page * input.limit,
          take: input.limit,
          orderBy: { createdAt: "desc" },
          include: { _count: { select: { items: true, runs: true } } },
        }),
        prisma.dataset.count({ where }),
      ]);

      return {
        data: data.map((d) => ({ ...d, itemsCount: d._count.items, runsCount: d._count.runs })),
        totalCount,
        page: input.page,
        limit: input.limit,
      };
    }),

  create: authedProcedure
    .input(
      z.object({
        projectId: z.string().optional(),
        name: z.string().min(1).max(100),
        description: z.string().optional(),
        inputSchema: z.record(z.unknown()).optional(),
        expectedOutputSchema: z.record(z.unknown()).optional(),
        metadata: z.record(z.unknown()).optional(),
      })
    )
    .mutation(async ({ input, ctx }) => {
      const projectId = ctx.projectId ?? input.projectId;
      if (!projectId) throw new TRPCError({ code: "BAD_REQUEST", message: "projectId required" });
      try {
        const ds = await prisma.dataset.create({
          data: {
            projectId,
            name: input.name,
            description: input.description,
            inputSchema: input.inputSchema as never,
            expectedOutputSchema: input.expectedOutputSchema as never,
            metadata: input.metadata as never,
          },
        });
        return ds;
      } catch (err) {
        if (err instanceof Error && err.message.includes("Unique constraint"))
          throw new TRPCError({ code: "CONFLICT", message: "Dataset name already exists" });
        throw err;
      }
    }),

  update: authedProcedure
    .input(
      z.object({
        projectId: z.string().optional(),
        id: z.string(),
        name: z.string().min(1).max(100).optional(),
        description: z.string().optional(),
        inputSchema: z.record(z.unknown()).optional(),
        expectedOutputSchema: z.record(z.unknown()).optional(),
        metadata: z.record(z.unknown()).optional(),
      })
    )
    .mutation(async ({ input, ctx }) => {
      const projectId = ctx.projectId ?? input.projectId;
      if (!projectId) throw new TRPCError({ code: "BAD_REQUEST", message: "projectId required" });
      const data: Record<string, unknown> = {};
      if (input.name !== undefined) data.name = input.name;
      if (input.description !== undefined) data.description = input.description;
      if (input.inputSchema !== undefined) data.inputSchema = input.inputSchema;
      if (input.expectedOutputSchema !== undefined)
        data.expectedOutputSchema = input.expectedOutputSchema;
      if (input.metadata !== undefined) data.metadata = input.metadata;
      return prisma.dataset.update({ where: { id: input.id }, data });
    }),

  delete: authedProcedure
    .input(z.object({ projectId: z.string().optional(), id: z.string() }))
    .mutation(async ({ input, ctx }) => {
      const projectId = ctx.projectId ?? input.projectId;
      if (!projectId) throw new TRPCError({ code: "BAD_REQUEST", message: "projectId required" });
      await prisma.dataset.delete({ where: { id: input.id } });
      return { success: true };
    }),

  detail: authedProcedure
    .input(z.object({ projectId: z.string().optional(), id: z.string() }))
    .query(async ({ input, ctx }) => {
      const projectId = ctx.projectId ?? input.projectId;
      if (!projectId) throw new TRPCError({ code: "BAD_REQUEST", message: "projectId required" });
      const ds = await prisma.dataset.findUnique({
        where: { id: input.id },
        include: { _count: { select: { items: true, runs: true } } },
      });
      if (!ds) throw new TRPCError({ code: "NOT_FOUND", message: "Dataset not found" });
      return { ...ds, itemsCount: ds._count.items, runsCount: ds._count.runs };
    }),
});
```

- [ ] **Step 2: Register in root**

Modify `web/src/server/api/root.ts`:

Add import:

```typescript
import { datasetsRouter } from "./routers/datasets";
```

Add to appRouter:

```typescript
export const appRouter = createTRPCRouter({
  health: publicProcedure.query(() => ({ status: "ok" })),
  traces: tracesRouter,
  prompts: promptsRouter,
  metrics: metricsRouter,
  scores: scoresRouter,
  evals: evalsRouter,
  annotations: annotationsRouter,
  datasets: datasetsRouter,
});
```

- [ ] **Step 3: Typecheck**

```bash
pnpm --filter web run typecheck
```

Expected: passes.

- [ ] **Step 4: Commit**

```bash
git add web/src/server/api/routers/datasets.ts web/src/server/api/root.ts
git commit -m "feat(web): add datasets tRPC router"
```

---

### Task 4.2: datasetItems router

**Files:**

- Create: `web/src/server/api/routers/datasetItems.ts`

- [ ] **Step 1: Write router**

Create `web/src/server/api/routers/datasetItems.ts`:

```typescript
import { z } from "zod";
import { TRPCError } from "@trpc/server";
import { authedProcedure, createTRPCRouter } from "../trpc";
import { prisma } from "@constell/shared/src/db";

export const datasetItemsRouter = createTRPCRouter({
  list: authedProcedure
    .input(
      z.object({
        projectId: z.string().optional(),
        datasetId: z.string(),
        status: z.enum(["ACTIVE", "ARCHIVED"]).optional(),
        page: z.number().default(0),
        limit: z.number().default(50),
      })
    )
    .query(async ({ input, ctx }) => {
      const projectId = ctx.projectId ?? input.projectId;
      if (!projectId) throw new TRPCError({ code: "BAD_REQUEST", message: "projectId required" });
      const where = { projectId, datasetId: input.datasetId, status: input.status };
      const [data, totalCount] = await Promise.all([
        prisma.datasetItem.findMany({
          where,
          skip: input.page * input.limit,
          take: input.limit,
          orderBy: { createdAt: "asc" },
        }),
        prisma.datasetItem.count({ where }),
      ]);
      return { data, totalCount, page: input.page, limit: input.limit };
    }),

  create: authedProcedure
    .input(
      z.object({
        projectId: z.string().optional(),
        datasetId: z.string(),
        input: z.any().optional(),
        expectedOutput: z.any().optional(),
        metadata: z.record(z.unknown()).optional(),
        status: z.enum(["ACTIVE", "ARCHIVED"]).optional(),
      })
    )
    .mutation(async ({ input, ctx }) => {
      const projectId = ctx.projectId ?? input.projectId;
      if (!projectId) throw new TRPCError({ code: "BAD_REQUEST", message: "projectId required" });
      return prisma.datasetItem.create({
        data: {
          projectId,
          datasetId: input.datasetId,
          input: input.input as never,
          expectedOutput: input.expectedOutput as never,
          metadata: input.metadata as never,
          status: input.status,
        },
      });
    }),

  createMany: authedProcedure
    .input(
      z.object({
        projectId: z.string().optional(),
        datasetId: z.string(),
        items: z.array(
          z.object({
            input: z.any().optional(),
            expectedOutput: z.any().optional(),
            metadata: z.record(z.unknown()).optional(),
          })
        ),
      })
    )
    .mutation(async ({ input, ctx }) => {
      const projectId = ctx.projectId ?? input.projectId;
      if (!projectId) throw new TRPCError({ code: "BAD_REQUEST", message: "projectId required" });
      const result = await prisma.datasetItem.createMany({
        data: input.items.map((it) => ({
          projectId,
          datasetId: input.datasetId,
          input: it.input as never,
          expectedOutput: it.expectedOutput as never,
          metadata: it.metadata as never,
        })),
      });
      return { createdCount: result.count };
    }),

  createFromTrace: authedProcedure
    .input(
      z.object({
        projectId: z.string().optional(),
        datasetId: z.string(),
        traceId: z.string(),
        observationId: z.string().optional(),
        input: z.any().optional(),
        expectedOutput: z.any().optional(),
      })
    )
    .mutation(async ({ input, ctx }) => {
      const projectId = ctx.projectId ?? input.projectId;
      if (!projectId) throw new TRPCError({ code: "BAD_REQUEST", message: "projectId required" });

      let itemInput = input.input;
      let itemExpectedOutput = input.expectedOutput;

      if (itemInput === undefined || itemExpectedOutput === undefined) {
        if (input.observationId) {
          const obs = await prisma.observation.findFirst({
            where: { id: input.observationId, projectId },
          });
          if (!obs) throw new TRPCError({ code: "NOT_FOUND", message: "Observation not found" });
          itemInput = itemInput ?? obs.input;
          itemExpectedOutput = itemExpectedOutput ?? obs.output;
        } else {
          const trace = await prisma.trace.findFirst({ where: { id: input.traceId, projectId } });
          if (!trace) throw new TRPCError({ code: "NOT_FOUND", message: "Trace not found" });
          itemInput = itemInput ?? trace.metadata;
        }
      }

      return prisma.datasetItem.create({
        data: {
          projectId,
          datasetId: input.datasetId,
          input: itemInput as never,
          expectedOutput: itemExpectedOutput as never,
          sourceTraceId: input.traceId,
          sourceObservationId: input.observationId,
        },
      });
    }),

  update: authedProcedure
    .input(
      z.object({
        projectId: z.string().optional(),
        id: z.string(),
        input: z.any().optional(),
        expectedOutput: z.any().optional(),
        metadata: z.record(z.unknown()).optional(),
        status: z.enum(["ACTIVE", "ARCHIVED"]).optional(),
      })
    )
    .mutation(async ({ input, ctx }) => {
      const projectId = ctx.projectId ?? input.projectId;
      if (!projectId) throw new TRPCError({ code: "BAD_REQUEST", message: "projectId required" });
      const data: Record<string, unknown> = {};
      if (input.input !== undefined) data.input = input.input;
      if (input.expectedOutput !== undefined) data.expectedOutput = input.expectedOutput;
      if (input.metadata !== undefined) data.metadata = input.metadata;
      if (input.status !== undefined) data.status = input.status;
      return prisma.datasetItem.update({ where: { id: input.id }, data });
    }),

  delete: authedProcedure
    .input(z.object({ projectId: z.string().optional(), id: z.string() }))
    .mutation(async ({ input, ctx }) => {
      const projectId = ctx.projectId ?? input.projectId;
      if (!projectId) throw new TRPCError({ code: "BAD_REQUEST", message: "projectId required" });
      await prisma.datasetItem.delete({ where: { id: input.id } });
      return { success: true };
    }),
});
```

- [ ] **Step 2: Register in root**

Modify `web/src/server/api/root.ts`:

Add import:

```typescript
import { datasetItemsRouter } from "./routers/datasetItems";
```

Add to appRouter:

```typescript
  datasetItems: datasetItemsRouter,
```

- [ ] **Step 3: Typecheck + commit**

```bash
pnpm --filter web run typecheck
git add web/src/server/api/routers/datasetItems.ts web/src/server/api/root.ts
git commit -m "feat(web): add datasetItems tRPC router"
```

---

### Task 4.3: datasetRunPresets router

**Files:**

- Create: `web/src/server/api/routers/datasetRunPresets.ts`

- [ ] **Step 1: Write router**

Create `web/src/server/api/routers/datasetRunPresets.ts`:

```typescript
import { z } from "zod";
import { TRPCError } from "@trpc/server";
import { authedProcedure, createTRPCRouter } from "../trpc";
import { prisma } from "@constell/shared/src/db";

export const datasetRunPresetsRouter = createTRPCRouter({
  list: authedProcedure
    .input(z.object({ projectId: z.string().optional() }))
    .query(async ({ input, ctx }) => {
      const projectId = ctx.projectId ?? input.projectId;
      if (!projectId) throw new TRPCError({ code: "BAD_REQUEST", message: "projectId required" });
      return prisma.datasetRunPreset.findMany({
        where: { projectId },
        orderBy: { createdAt: "desc" },
      });
    }),

  create: authedProcedure
    .input(
      z.object({
        projectId: z.string().optional(),
        name: z.string().min(1),
        description: z.string().optional(),
        promptId: z.string().optional(),
        promptVersion: z.number().optional(),
        model: z.string().min(1),
        modelParams: z.record(z.unknown()).optional(),
        evalTemplateIds: z.array(z.string()).default([]),
      })
    )
    .mutation(async ({ input, ctx }) => {
      const projectId = ctx.projectId ?? input.projectId;
      if (!projectId) throw new TRPCError({ code: "BAD_REQUEST", message: "projectId required" });
      try {
        return prisma.datasetRunPreset.create({
          data: {
            projectId,
            name: input.name,
            description: input.description,
            promptId: input.promptId,
            promptVersion: input.promptVersion,
            model: input.model,
            modelParams: input.modelParams as never,
            evalTemplateIds: input.evalTemplateIds,
          },
        });
      } catch (err) {
        if (err instanceof Error && err.message.includes("Unique constraint"))
          throw new TRPCError({ code: "CONFLICT", message: "Preset name already exists" });
        throw err;
      }
    }),

  update: authedProcedure
    .input(
      z.object({
        projectId: z.string().optional(),
        id: z.string(),
        name: z.string().min(1).optional(),
        description: z.string().optional(),
        promptId: z.string().optional(),
        promptVersion: z.number().optional(),
        model: z.string().min(1).optional(),
        modelParams: z.record(z.unknown()).optional(),
        evalTemplateIds: z.array(z.string()).optional(),
      })
    )
    .mutation(async ({ input, ctx }) => {
      const projectId = ctx.projectId ?? input.projectId;
      if (!projectId) throw new TRPCError({ code: "BAD_REQUEST", message: "projectId required" });
      const data: Record<string, unknown> = {};
      if (input.name !== undefined) data.name = input.name;
      if (input.description !== undefined) data.description = input.description;
      if (input.promptId !== undefined) data.promptId = input.promptId;
      if (input.promptVersion !== undefined) data.promptVersion = input.promptVersion;
      if (input.model !== undefined) data.model = input.model;
      if (input.modelParams !== undefined) data.modelParams = input.modelParams;
      if (input.evalTemplateIds !== undefined) data.evalTemplateIds = input.evalTemplateIds;
      return prisma.datasetRunPreset.update({ where: { id: input.id }, data });
    }),

  delete: authedProcedure
    .input(z.object({ projectId: z.string().optional(), id: z.string() }))
    .mutation(async ({ input, ctx }) => {
      const projectId = ctx.projectId ?? input.projectId;
      if (!projectId) throw new TRPCError({ code: "BAD_REQUEST", message: "projectId required" });
      await prisma.datasetRunPreset.delete({ where: { id: input.id } });
      return { success: true };
    }),
});
```

- [ ] **Step 2: Register in root**

Modify `web/src/server/api/root.ts`:

Add import:

```typescript
import { datasetRunPresetsRouter } from "./routers/datasetRunPresets";
```

Add to appRouter:

```typescript
  datasetRunPresets: datasetRunPresetsRouter,
```

- [ ] **Step 3: Typecheck + commit**

```bash
pnpm --filter web run typecheck
git add web/src/server/api/routers/datasetRunPresets.ts web/src/server/api/root.ts
git commit -m "feat(web): add datasetRunPresets tRPC router"
```

---

### Task 4.4: datasetRuns router

**Files:**

- Create: `web/src/server/api/routers/datasetRuns.ts`
- Modify: `web/src/server/api/root.ts`

- [ ] **Step 1: Write router**

Create `web/src/server/api/routers/datasetRuns.ts`:

```typescript
import { z } from "zod";
import { TRPCError } from "@trpc/server";
import { authedProcedure, createTRPCRouter } from "../trpc";
import { prisma } from "@constell/shared/src/db";
import { queueNames, type DatasetRunJob } from "@constell/shared/src/server";
import { Queue } from "bullmq";
import Redis from "ioredis";

const redis = new Redis({
  host: process.env.REDIS_HOST || "localhost",
  port: Number(process.env.REDIS_PORT) || 6379,
  password: process.env.REDIS_AUTH,
  maxRetriesPerRequest: null,
});

const datasetRunQueue = new Queue<DatasetRunJob>(queueNames.datasetRun, { connection: redis });

export const datasetRunsRouter = createTRPCRouter({
  list: authedProcedure
    .input(
      z.object({
        projectId: z.string().optional(),
        datasetId: z.string(),
        page: z.number().default(0),
        limit: z.number().default(20),
      })
    )
    .query(async ({ input, ctx }) => {
      const projectId = ctx.projectId ?? input.projectId;
      if (!projectId) throw new TRPCError({ code: "BAD_REQUEST", message: "projectId required" });
      const where = { projectId, datasetId: input.datasetId };
      const [data, totalCount] = await Promise.all([
        prisma.datasetRun.findMany({
          where,
          skip: input.page * input.limit,
          take: input.limit,
          orderBy: { createdAt: "desc" },
        }),
        prisma.datasetRun.count({ where }),
      ]);
      return { data, totalCount, page: input.page, limit: input.limit };
    }),

  create: authedProcedure
    .input(
      z
        .object({
          projectId: z.string().optional(),
          datasetId: z.string(),
          name: z.string().min(1),
          description: z.string().optional(),
          presetId: z.string().optional(),
          promptId: z.string().optional(),
          promptVersion: z.number().optional(),
          model: z.string().optional(),
          modelParams: z.record(z.unknown()).optional(),
          evalTemplateIds: z.array(z.string()).optional(),
        })
        .refine(
          (data) => {
            if (!data.presetId && !data.model) return false;
            return true;
          },
          { message: "Must provide presetId or manual model config", path: ["model"] }
        )
    )
    .mutation(async ({ input, ctx }) => {
      const projectId = ctx.projectId ?? input.projectId;
      if (!projectId) throw new TRPCError({ code: "BAD_REQUEST", message: "projectId required" });

      let execParams: Omit<DatasetRunJob, "projectId" | "datasetId" | "datasetRunId">;

      if (input.presetId) {
        const preset = await prisma.datasetRunPreset.findFirst({
          where: { id: input.presetId, projectId },
        });
        if (!preset) throw new TRPCError({ code: "NOT_FOUND", message: "Preset not found" });
        execParams = {
          promptId: preset.promptId ?? undefined,
          promptVersion: preset.promptVersion ?? undefined,
          model: preset.model,
          modelParams: (preset.modelParams as Record<string, unknown>) ?? undefined,
          evalTemplateIds: preset.evalTemplateIds,
        };
      } else {
        execParams = {
          promptId: input.promptId ?? undefined,
          promptVersion: input.promptVersion ?? undefined,
          model: input.model!,
          modelParams: input.modelParams ?? undefined,
          evalTemplateIds: input.evalTemplateIds ?? [],
        };
      }

      try {
        const run = await prisma.datasetRun.create({
          data: {
            projectId,
            datasetId: input.datasetId,
            name: input.name,
            description: input.description,
            status: "PENDING",
            metadata: { ...execParams, presetId: input.presetId },
          },
        });

        await datasetRunQueue.add(
          run.id,
          {
            projectId,
            datasetId: input.datasetId,
            datasetRunId: run.id,
            ...execParams,
          },
          { jobId: run.id }
        );

        return run;
      } catch (err) {
        if (err instanceof Error && err.message.includes("Unique constraint"))
          throw new TRPCError({
            code: "CONFLICT",
            message: "Run name already exists for this dataset",
          });
        throw err;
      }
    }),

  cancel: authedProcedure
    .input(z.object({ projectId: z.string().optional(), id: z.string() }))
    .mutation(async ({ input, ctx }) => {
      const projectId = ctx.projectId ?? input.projectId;
      if (!projectId) throw new TRPCError({ code: "BAD_REQUEST", message: "projectId required" });

      const job = await datasetRunQueue.getJob(input.id);
      if (job) {
        await job.remove();
      }

      return prisma.datasetRun.update({ where: { id: input.id }, data: { status: "CANCELLED" } });
    }),

  delete: authedProcedure
    .input(z.object({ projectId: z.string().optional(), id: z.string() }))
    .mutation(async ({ input, ctx }) => {
      const projectId = ctx.projectId ?? input.projectId;
      if (!projectId) throw new TRPCError({ code: "BAD_REQUEST", message: "projectId required" });
      await prisma.datasetRun.delete({ where: { id: input.id } });
      return { success: true };
    }),

  detail: authedProcedure
    .input(z.object({ projectId: z.string().optional(), id: z.string() }))
    .query(async ({ input, ctx }) => {
      const projectId = ctx.projectId ?? input.projectId;
      if (!projectId) throw new TRPCError({ code: "BAD_REQUEST", message: "projectId required" });
      const run = await prisma.datasetRun.findUnique({
        where: { id: input.id },
        include: { runItems: { include: { datasetItem: true } } },
      });
      if (!run) throw new TRPCError({ code: "NOT_FOUND", message: "Run not found" });
      return run;
    }),

  items: authedProcedure
    .input(
      z.object({
        projectId: z.string().optional(),
        runId: z.string(),
        page: z.number().default(0),
        limit: z.number().default(50),
      })
    )
    .query(async ({ input, ctx }) => {
      const projectId = ctx.projectId ?? input.projectId;
      if (!projectId) throw new TRPCError({ code: "BAD_REQUEST", message: "projectId required" });
      const where = { projectId, datasetRunId: input.runId };
      const [data, totalCount] = await Promise.all([
        prisma.datasetRunItem.findMany({
          where,
          skip: input.page * input.limit,
          take: input.limit,
          include: { datasetItem: true },
        }),
        prisma.datasetRunItem.count({ where }),
      ]);
      return { data, totalCount, page: input.page, limit: input.limit };
    }),

  compare: authedProcedure
    .input(
      z.object({
        projectId: z.string().optional(),
        datasetId: z.string(),
        runIds: z.array(z.string()).min(2).max(5),
      })
    )
    .query(async ({ input, ctx }) => {
      const projectId = ctx.projectId ?? input.projectId;
      if (!projectId) throw new TRPCError({ code: "BAD_REQUEST", message: "projectId required" });

      const [datasetItems, runs] = await Promise.all([
        prisma.datasetItem.findMany({
          where: { projectId, datasetId: input.datasetId, status: "ACTIVE" },
          orderBy: { createdAt: "asc" },
        }),
        prisma.datasetRun.findMany({
          where: { projectId, id: { in: input.runIds }, datasetId: input.datasetId },
          include: { runItems: { include: { datasetItem: true } } },
        }),
      ]);

      const enrichedItems = datasetItems.map((item) => {
        const runsMap: Record<string, unknown> = {};
        for (const run of runs) {
          const ri = run.runItems.find((r) => r.datasetItemId === item.id);
          if (ri) {
            runsMap[run.id] = {
              traceId: ri.traceId,
              observationId: ri.observationId,
            };
          }
        }
        return { ...item, runs: runsMap };
      });

      return {
        datasetItems: enrichedItems,
        runs: runs.map((r) => ({ id: r.id, name: r.name, status: r.status, metadata: r.metadata })),
      };
    }),
});
```

- [ ] **Step 2: Register in root**

Modify `web/src/server/api/root.ts`:

Add import:

```typescript
import { datasetRunsRouter } from "./routers/datasetRuns";
```

Add to appRouter:

```typescript
  datasetRuns: datasetRunsRouter,
```

- [ ] **Step 3: Typecheck + commit**

```bash
pnpm --filter web run typecheck
git add web/src/server/api/routers/datasetRuns.ts web/src/server/api/root.ts
git commit -m "feat(web): add datasetRuns tRPC router with execute and compare"
```

---

## Workflow 5: Public REST API

> REST endpoints for SDK consumption. Follow existing public-api patterns.

### Task 5.1: Dataset REST endpoints

**Files:**

- Create: `web/src/pages/api/public/datasets/index.ts`
- Create: `web/src/pages/api/public/datasets/[name]/index.ts`

- [ ] **Step 1: Write endpoints**

Create `web/src/pages/api/public/datasets/index.ts`:

```typescript
import type { NextApiRequest, NextApiResponse } from "next";
import { prisma } from "@constell/shared/src/db";
import { validateApiKey } from "~/features/public-api/server/apiKeyAuth";

export default async function handler(req: NextApiRequest, res: NextApiResponse) {
  try {
    const apiKey = await validateApiKey(req.headers.authorization ?? "");
    const projectId = apiKey.projectId;

    if (req.method === "POST") {
      const { name, description, inputSchema, expectedOutputSchema, metadata } = req.body;
      const ds = await prisma.dataset.create({
        data: { projectId, name, description, inputSchema, expectedOutputSchema, metadata },
      });
      return res.status(201).json(ds);
    }

    if (req.method === "GET") {
      const { page = "0", limit = "20", search } = req.query;
      const where = {
        projectId,
        name: search ? { contains: String(search), mode: "insensitive" as const } : undefined,
      };
      const [data, totalCount] = await Promise.all([
        prisma.dataset.findMany({
          where,
          skip: Number(page) * Number(limit),
          take: Number(limit),
          orderBy: { createdAt: "desc" },
        }),
        prisma.dataset.count({ where }),
      ]);
      return res
        .status(200)
        .json({ data, meta: { totalCount, page: Number(page), limit: Number(limit) } });
    }

    return res.status(405).json({ error: "Method not allowed" });
  } catch (err) {
    return res.status(500).json({ error: (err as Error).message });
  }
}
```

Create `web/src/pages/api/public/datasets/[name]/index.ts`:

```typescript
import type { NextApiRequest, NextApiResponse } from "next";
import { prisma } from "@constell/shared/src/db";
import { validateApiKey } from "~/features/public-api/server/apiKeyAuth";

export default async function handler(req: NextApiRequest, res: NextApiResponse) {
  try {
    const apiKey = await validateApiKey(req.headers.authorization ?? "");
    const projectId = apiKey.projectId;
    const name = String(req.query.name);

    if (req.method === "GET") {
      const ds = await prisma.dataset.findUnique({
        where: { projectId_name: { projectId, name } },
        include: { items: true, runs: true },
      });
      if (!ds) return res.status(404).json({ error: "Dataset not found" });
      return res.status(200).json(ds);
    }

    if (req.method === "DELETE") {
      await prisma.dataset.delete({ where: { projectId_name: { projectId, name } } });
      return res.status(200).json({ message: "Dataset deleted" });
    }

    return res.status(405).json({ error: "Method not allowed" });
  } catch (err) {
    return res.status(500).json({ error: (err as Error).message });
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add web/src/pages/api/public/datasets/
git commit -m "feat(web): add public REST endpoints for datasets"
```

---

### Task 5.2: Dataset items + runs REST endpoints

**Files:**

- Create: `web/src/pages/api/public/dataset-items/index.ts`
- Create: `web/src/pages/api/public/dataset-items/[id].ts`
- Create: `web/src/pages/api/public/dataset-runs/index.ts`
- Create: `web/src/pages/api/public/datasets/[name]/runs/index.ts`
- Create: `web/src/pages/api/public/datasets/[name]/runs/[runName].ts`

- [ ] **Step 1: Write dataset-items endpoints**

Create `web/src/pages/api/public/dataset-items/index.ts`:

```typescript
import type { NextApiRequest, NextApiResponse } from "next";
import { prisma } from "@constell/shared/src/db";
import { validateApiKey } from "~/features/public-api/server/apiKeyAuth";

export default async function handler(req: NextApiRequest, res: NextApiResponse) {
  try {
    const apiKey = await validateApiKey(req.headers.authorization ?? "");
    const projectId = apiKey.projectId;

    if (req.method === "POST") {
      const { datasetName, input, expectedOutput, metadata, sourceTraceId, sourceObservationId } =
        req.body;
      const dataset = await prisma.dataset.findUnique({
        where: { projectId_name: { projectId, name: datasetName } },
      });
      if (!dataset) return res.status(404).json({ error: "Dataset not found" });
      const item = await prisma.datasetItem.create({
        data: {
          projectId,
          datasetId: dataset.id,
          input,
          expectedOutput,
          metadata,
          sourceTraceId,
          sourceObservationId,
        },
      });
      return res.status(201).json(item);
    }

    if (req.method === "GET") {
      const { datasetName, page = "0", limit = "50" } = req.query;
      const where: Record<string, unknown> = { projectId };
      if (datasetName) {
        const dataset = await prisma.dataset.findUnique({
          where: { projectId_name: { projectId, name: String(datasetName) } },
        });
        if (!dataset) return res.status(404).json({ error: "Dataset not found" });
        where.datasetId = dataset.id;
      }
      const [data, totalCount] = await Promise.all([
        prisma.datasetItem.findMany({
          where,
          skip: Number(page) * Number(limit),
          take: Number(limit),
          orderBy: { createdAt: "asc" },
        }),
        prisma.datasetItem.count({ where }),
      ]);
      return res
        .status(200)
        .json({ data, meta: { totalCount, page: Number(page), limit: Number(limit) } });
    }

    return res.status(405).json({ error: "Method not allowed" });
  } catch (err) {
    return res.status(500).json({ error: (err as Error).message });
  }
}
```

Create `web/src/pages/api/public/dataset-items/[id].ts`:

```typescript
import type { NextApiRequest, NextApiResponse } from "next";
import { prisma } from "@constell/shared/src/db";
import { validateApiKey } from "~/features/public-api/server/apiKeyAuth";

export default async function handler(req: NextApiRequest, res: NextApiResponse) {
  try {
    const apiKey = await validateApiKey(req.headers.authorization ?? "");
    const projectId = apiKey.projectId;
    const id = String(req.query.id);

    if (req.method === "GET") {
      const item = await prisma.datasetItem.findFirst({ where: { id, projectId } });
      if (!item) return res.status(404).json({ error: "Item not found" });
      return res.status(200).json(item);
    }

    if (req.method === "DELETE") {
      await prisma.datasetItem.deleteMany({ where: { id, projectId } });
      return res.status(200).json({ message: "Item deleted" });
    }

    return res.status(405).json({ error: "Method not allowed" });
  } catch (err) {
    return res.status(500).json({ error: (err as Error).message });
  }
}
```

- [ ] **Step 2: Write dataset-runs endpoints**

Create `web/src/pages/api/public/dataset-runs/index.ts`:

```typescript
import type { NextApiRequest, NextApiResponse } from "next";
import { prisma } from "@constell/shared/src/db";
import { validateApiKey } from "~/features/public-api/server/apiKeyAuth";
import { queueNames, type DatasetRunJob } from "@constell/shared/src/server";
import { Queue } from "bullmq";
import Redis from "ioredis";

const redis = new Redis({
  host: process.env.REDIS_HOST || "localhost",
  port: Number(process.env.REDIS_PORT) || 6379,
  password: process.env.REDIS_AUTH,
  maxRetriesPerRequest: null,
});
const datasetRunQueue = new Queue<DatasetRunJob>(queueNames.datasetRun, { connection: redis });

export default async function handler(req: NextApiRequest, res: NextApiResponse) {
  try {
    const apiKey = await validateApiKey(req.headers.authorization ?? "");
    const projectId = apiKey.projectId;

    if (req.method === "POST") {
      const {
        datasetName,
        runName,
        description,
        presetName,
        promptName,
        promptVersion,
        model,
        modelParams,
        evalTemplateNames,
      } = req.body;
      const dataset = await prisma.dataset.findUnique({
        where: { projectId_name: { projectId, name: datasetName } },
      });
      if (!dataset) return res.status(404).json({ error: "Dataset not found" });

      let execParams: {
        model: string;
        promptId?: string;
        promptVersion?: number;
        modelParams?: Record<string, unknown>;
        evalTemplateIds: string[];
      };

      if (presetName) {
        const preset = await prisma.datasetRunPreset.findUnique({
          where: { projectId_name: { projectId, name: presetName } },
        });
        if (!preset) return res.status(404).json({ error: "Preset not found" });
        execParams = {
          model: preset.model,
          promptId: preset.promptId ?? undefined,
          promptVersion: preset.promptVersion ?? undefined,
          modelParams: (preset.modelParams as Record<string, unknown>) ?? undefined,
          evalTemplateIds: preset.evalTemplateIds,
        };
      } else if (model) {
        let promptId: string | undefined;
        if (promptName) {
          const prompt = await prisma.prompt.findUnique({
            where: { projectId_name: { projectId, name: promptName } },
          });
          if (prompt) promptId = prompt.id;
        }
        const evalIds: string[] = [];
        if (evalTemplateNames?.length) {
          for (const etName of evalTemplateNames) {
            const et = await prisma.evalTemplate.findUnique({
              where: { projectId_name: { projectId, name: etName } },
            });
            if (et) evalIds.push(et.id);
          }
        }
        execParams = { model, promptId, promptVersion, modelParams, evalTemplateIds: evalIds };
      } else {
        return res.status(400).json({ error: "Must provide presetName or model" });
      }

      const run = await prisma.datasetRun.create({
        data: {
          projectId,
          datasetId: dataset.id,
          name: runName,
          description,
          status: "PENDING",
          metadata: { ...execParams, presetName },
        },
      });

      await datasetRunQueue.add(
        run.id,
        { projectId, datasetId: dataset.id, datasetRunId: run.id, ...execParams },
        { jobId: run.id }
      );
      return res.status(201).json(run);
    }

    return res.status(405).json({ error: "Method not allowed" });
  } catch (err) {
    return res.status(500).json({ error: (err as Error).message });
  }
}
```

Create `web/src/pages/api/public/datasets/[name]/runs/index.ts`:

```typescript
import type { NextApiRequest, NextApiResponse } from "next";
import { prisma } from "@constell/shared/src/db";
import { validateApiKey } from "~/features/public-api/server/apiKeyAuth";

export default async function handler(req: NextApiRequest, res: NextApiResponse) {
  try {
    const apiKey = await validateApiKey(req.headers.authorization ?? "");
    const projectId = apiKey.projectId;
    const name = String(req.query.name);
    const dataset = await prisma.dataset.findUnique({
      where: { projectId_name: { projectId, name } },
    });
    if (!dataset) return res.status(404).json({ error: "Dataset not found" });

    if (req.method === "GET") {
      const { page = "0", limit = "20" } = req.query;
      const [data, totalCount] = await Promise.all([
        prisma.datasetRun.findMany({
          where: { projectId, datasetId: dataset.id },
          skip: Number(page) * Number(limit),
          take: Number(limit),
          orderBy: { createdAt: "desc" },
        }),
        prisma.datasetRun.count({ where: { projectId, datasetId: dataset.id } }),
      ]);
      return res
        .status(200)
        .json({ data, meta: { totalCount, page: Number(page), limit: Number(limit) } });
    }

    return res.status(405).json({ error: "Method not allowed" });
  } catch (err) {
    return res.status(500).json({ error: (err as Error).message });
  }
}
```

Create `web/src/pages/api/public/datasets/[name]/runs/[runName].ts`:

```typescript
import type { NextApiRequest, NextApiResponse } from "next";
import { prisma } from "@constell/shared/src/db";
import { validateApiKey } from "~/features/public-api/server/apiKeyAuth";

export default async function handler(req: NextApiRequest, res: NextApiResponse) {
  try {
    const apiKey = await validateApiKey(req.headers.authorization ?? "");
    const projectId = apiKey.projectId;
    const name = String(req.query.name);
    const runName = String(req.query.runName);
    const dataset = await prisma.dataset.findUnique({
      where: { projectId_name: { projectId, name } },
    });
    if (!dataset) return res.status(404).json({ error: "Dataset not found" });

    if (req.method === "GET") {
      const run = await prisma.datasetRun.findUnique({
        where: { datasetId_projectId_name: { datasetId: dataset.id, projectId, name: runName } },
        include: { runItems: { include: { datasetItem: true } } },
      });
      if (!run) return res.status(404).json({ error: "Run not found" });
      return res.status(200).json(run);
    }

    if (req.method === "DELETE") {
      await prisma.datasetRun.delete({
        where: { datasetId_projectId_name: { datasetId: dataset.id, projectId, name: runName } },
      });
      return res.status(200).json({ message: "Run deleted" });
    }

    return res.status(405).json({ error: "Method not allowed" });
  } catch (err) {
    return res.status(500).json({ error: (err as Error).message });
  }
}
```

- [ ] **Step 3: Typecheck + commit**

```bash
pnpm --filter web run typecheck
git add web/src/pages/api/public/dataset-items/ web/src/pages/api/public/dataset-runs/ web/src/pages/api/public/datasets/
git commit -m "feat(web): add public REST endpoints for dataset items and runs"
```

---

## Workflow 6: UI Components and Pages

> Build frontend using existing patterns: shadcn/ui components, tRPC hooks, Tailwind.

### Task 6.1: Dataset list and detail pages

**Files:**

- Create: `web/src/features/datasets/hooks/useDatasets.ts`
- Create: `web/src/features/datasets/components/DatasetTable.tsx`
- Create: `web/src/features/datasets/components/DatasetForm.tsx`
- Create: `web/src/pages/datasets/index.tsx`
- Create: `web/src/pages/datasets/new.tsx`
- Create: `web/src/pages/datasets/[id].tsx`

- [ ] **Step 1: Write hooks**

Create `web/src/features/datasets/hooks/useDatasets.ts`:

```typescript
import { api } from "~/utils/api";

export function useDatasetList(
  projectId: string | undefined,
  search?: string,
  page = 0,
  limit = 20
) {
  return api.datasets.list.useQuery(
    { projectId: projectId ?? "", search, page, limit },
    { enabled: !!projectId }
  );
}

export function useDatasetDetail(projectId: string | undefined, id: string) {
  return api.datasets.detail.useQuery(
    { projectId: projectId ?? "", id },
    { enabled: !!projectId && !!id }
  );
}

export function useDatasetCreate() {
  return api.datasets.create.useMutation();
}

export function useDatasetDelete() {
  const utils = api.useUtils();
  return api.datasets.delete.useMutation({
    onSuccess: () => utils.datasets.list.invalidate(),
  });
}
```

- [ ] **Step 2: Write DatasetTable component**

Create `web/src/features/datasets/components/DatasetTable.tsx`:

```typescript
import Link from "next/link";
import { Button } from "~/components/ui/button";

interface DatasetTableProps {
  projectId: string;
  datasets: Array<{
    id: string;
    name: string;
    description: string | null;
    itemsCount: number;
    runsCount: number;
    createdAt: Date;
  }>;
  onDelete: (id: string) => void;
}

export function DatasetTable({ projectId, datasets, onDelete }: DatasetTableProps) {
  return (
    <table className="w-full text-sm">
      <thead>
        <tr className="border-b">
          <th className="text-left py-2">Name</th>
          <th className="text-left py-2">Items</th>
          <th className="text-left py-2">Runs</th>
          <th className="text-left py-2">Actions</th>
        </tr>
      </thead>
      <tbody>
        {datasets.map((ds) => (
          <tr key={ds.id} className="border-b hover:bg-muted/50">
            <td className="py-2">
              <Link href={`/datasets/${ds.id}`} className="font-medium hover:underline">
                {ds.name}
              </Link>
              {ds.description && <p className="text-muted-foreground text-xs">{ds.description}</p>}
            </td>
            <td className="py-2">{ds.itemsCount}</td>
            <td className="py-2">{ds.runsCount}</td>
            <td className="py-2">
              <Button variant="ghost" size="sm" onClick={() => onDelete(ds.id)}>
                Delete
              </Button>
            </td>
          </tr>
        ))}
      </tbody>
    </table>
  );
}
```

- [ ] **Step 3: Write pages**

Create `web/src/pages/datasets/index.tsx`:

```typescript
import { useState } from "react";
import { useActiveProject } from "~/features/projects/hooks/useActiveProject";
import { useDatasetList, useDatasetDelete } from "~/features/datasets/hooks/useDatasets";
import { DatasetTable } from "~/features/datasets/components/DatasetTable";
import { Button } from "~/components/ui/button";
import { Input } from "~/components/ui/input";
import Link from "next/link";

export default function DatasetsPage() {
  const { projectId } = useActiveProject();
  const [search, setSearch] = useState("");
  const { data, isLoading } = useDatasetList(projectId ?? undefined, search || undefined);
  const deleteMutation = useDatasetDelete();

  return (
    <div className="container py-6">
      <div className="flex items-center justify-between mb-6">
        <h1 className="text-2xl font-bold">Datasets</h1>
        <Link href="/datasets/new">
          <Button>+ New Dataset</Button>
        </Link>
      </div>
      <Input placeholder="Search datasets..." value={search} onChange={(e) => setSearch(e.target.value)} className="mb-4 max-w-sm" />
      {isLoading ? <p>Loading...</p> : data ? (
        <DatasetTable projectId={projectId ?? ""} datasets={data.data} onDelete={(id) => deleteMutation.mutate({ projectId: projectId ?? "", id })} />
      ) : null}
    </div>
  );
}
```

Create `web/src/pages/datasets/new.tsx`:

```typescript
import { useState } from "react";
import { useRouter } from "next/router";
import { useActiveProject } from "~/features/projects/hooks/useActiveProject";
import { useDatasetCreate } from "~/features/datasets/hooks/useDatasets";
import { Button } from "~/components/ui/button";
import { Input } from "~/components/ui/input";
import { Textarea } from "~/components/ui/textarea";

export default function NewDatasetPage() {
  const { projectId } = useActiveProject();
  const router = useRouter();
  const create = useDatasetCreate();
  const [name, setName] = useState("");
  const [description, setDescription] = useState("");

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    if (!projectId) return;
    const ds = await create.mutateAsync({ projectId, name, description: description || undefined });
    router.push(`/datasets/${ds.id}`);
  };

  return (
    <div className="container py-6 max-w-lg">
      <h1 className="text-2xl font-bold mb-6">New Dataset</h1>
      <form onSubmit={handleSubmit} className="space-y-4">
        <div>
          <label className="text-sm font-medium">Name</label>
          <Input value={name} onChange={(e) => setName(e.target.value)} required />
        </div>
        <div>
          <label className="text-sm font-medium">Description</label>
          <Textarea value={description} onChange={(e) => setDescription(e.target.value)} />
        </div>
        <Button type="submit" disabled={create.isPending}>Create</Button>
      </form>
    </div>
  );
}
```

- [ ] **Step 4: Write dataset detail page (items + runs tabs)**

Create `web/src/pages/datasets/[id].tsx`:

```typescript
import { useState } from "react";
import { useRouter } from "next/router";
import Link from "next/link";
import { useActiveProject } from "~/features/projects/hooks/useActiveProject";
import { useDatasetDetail } from "~/features/datasets/hooks/useDatasets";
import { Button } from "~/components/ui/button";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "~/components/ui/tabs";

export default function DatasetDetailPage() {
  const router = useRouter();
  const { projectId } = useActiveProject();
  const id = String(router.query.id);
  const { data: dataset } = useDatasetDetail(projectId ?? undefined, id);
  const [tab, setTab] = useState("items");

  if (!dataset) return <div className="container py-6">Loading...</div>;

  return (
    <div className="container py-6">
      <div className="flex items-center justify-between mb-4">
        <div>
          <Link href="/datasets" className="text-sm text-muted-foreground hover:underline">← Datasets</Link>
          <h1 className="text-2xl font-bold mt-1">{dataset.name}</h1>
          {dataset.description && <p className="text-muted-foreground">{dataset.description}</p>}
        </div>
        <div className="flex gap-2">
          <Link href={`/datasets/${id}/items/new`}><Button variant="outline" size="sm">+ Item</Button></Link>
          <Link href={`/datasets/${id}/runs/new`}><Button size="sm">+ Run</Button></Link>
        </div>
      </div>

      <Tabs value={tab} onValueChange={setTab}>
        <TabsList>
          <TabsTrigger value="items">Items ({dataset.itemsCount})</TabsTrigger>
          <TabsTrigger value="runs">Runs ({dataset.runsCount})</TabsTrigger>
        </TabsList>
        <TabsContent value="items">
          <DatasetItemsTab projectId={projectId ?? ""} datasetId={id} />
        </TabsContent>
        <TabsContent value="runs">
          <DatasetRunsTab projectId={projectId ?? ""} datasetId={id} />
        </TabsContent>
      </Tabs>
    </div>
  );
}

function DatasetItemsTab({ projectId, datasetId }: { projectId: string; datasetId: string }) {
  const { data } = api.datasetItems.list.useQuery({ projectId, datasetId, limit: 50 });
  return (
    <div>
      {data?.data.map((item) => (
        <div key={item.id} className="border rounded p-3 mb-2 text-sm">
          <pre className="text-xs bg-muted p-2 rounded overflow-auto">{JSON.stringify(item.input, null, 2)}</pre>
          {item.expectedOutput && (
            <div className="mt-2">
              <span className="text-xs text-muted-foreground">Expected:</span>
              <pre className="text-xs bg-muted p-2 rounded overflow-auto">{JSON.stringify(item.expectedOutput, null, 2)}</pre>
            </div>
          )}
        </div>
      ))}
    </div>
  );
}

function DatasetRunsTab({ projectId, datasetId }: { projectId: string; datasetId: string }) {
  const { data } = api.datasetRuns.list.useQuery({ projectId, datasetId });
  const utils = api.useUtils();
  const deleteRun = api.datasetRuns.delete.useMutation({ onSuccess: () => utils.datasetRuns.list.invalidate() });

  return (
    <div>
      {data?.data.map((run) => (
        <div key={run.id} className="border rounded p-3 mb-2 flex items-center justify-between">
          <div>
            <Link href={`/datasets/${datasetId}/runs/${run.id}`} className="font-medium hover:underline">
              {run.name}
            </Link>
            <span className={`ml-2 text-xs px-2 py-0.5 rounded ${run.status === "COMPLETED" ? "bg-green-100 text-green-700" : run.status === "FAILED" ? "bg-red-100 text-red-700" : run.status === "RUNNING" ? "bg-blue-100 text-blue-700" : "bg-gray-100 text-gray-700"}`}>
              {run.status}
            </span>
          </div>
          <Button variant="ghost" size="sm" onClick={() => deleteRun.mutate({ projectId, id: run.id })}>Delete</Button>
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 5: Typecheck + commit**

```bash
pnpm --filter web run typecheck
git add web/src/features/datasets/ web/src/pages/datasets/
git commit -m "feat(web): add dataset list, create, and detail pages"
```

---

### Task 6.2: Run creation and comparison pages

**Files:**

- Create: `web/src/features/datasets/hooks/useDatasetRuns.ts`
- Create: `web/src/features/datasets/hooks/useDatasetRunPresets.ts`
- Create: `web/src/pages/datasets/[id]/runs/new.tsx`
- Create: `web/src/pages/datasets/[id]/compare.tsx`

- [ ] **Step 1: Write hooks**

Create `web/src/features/datasets/hooks/useDatasetRuns.ts`:

```typescript
import { api } from "~/utils/api";

export function useDatasetRunList(
  projectId: string | undefined,
  datasetId: string,
  page = 0,
  limit = 20
) {
  return api.datasetRuns.list.useQuery(
    { projectId: projectId ?? "", datasetId, page, limit },
    { enabled: !!projectId }
  );
}

export function useDatasetRunCreate() {
  const utils = api.useUtils();
  return api.datasetRuns.create.useMutation({
    onSuccess: (_, vars) => {
      utils.datasetRuns.list.invalidate({
        projectId: vars.projectId ?? "",
        datasetId: vars.datasetId,
      });
      utils.datasets.detail.invalidate({ projectId: vars.projectId ?? "", id: vars.datasetId });
    },
  });
}

export function useDatasetRunCompare(
  projectId: string | undefined,
  datasetId: string,
  runIds: string[]
) {
  return api.datasetRuns.compare.useQuery(
    { projectId: projectId ?? "", datasetId, runIds },
    { enabled: !!projectId && runIds.length >= 2 }
  );
}
```

Create `web/src/features/datasets/hooks/useDatasetRunPresets.ts`:

```typescript
import { api } from "~/utils/api";

export function usePresetList(projectId: string | undefined) {
  return api.datasetRunPresets.list.useQuery(
    { projectId: projectId ?? "" },
    { enabled: !!projectId }
  );
}

export function usePresetCreate() {
  const utils = api.useUtils();
  return api.datasetRunPresets.create.useMutation({
    onSuccess: (_, vars) =>
      utils.datasetRunPresets.list.invalidate({ projectId: vars.projectId ?? "" }),
  });
}
```

- [ ] **Step 2: Write Run creation page**

Create `web/src/pages/datasets/[id]/runs/new.tsx`:

```typescript
import { useState } from "react";
import { useRouter } from "next/router";
import { useActiveProject } from "~/features/projects/hooks/useActiveProject";
import { useDatasetRunCreate } from "~/features/datasets/hooks/useDatasetRuns";
import { usePresetList } from "~/features/datasets/hooks/useDatasetRunPresets";
import { usePromptList } from "~/features/prompts/hooks/usePrompts";
import { useEvalTemplateList } from "~/features/evals/hooks/useEvalTemplates";
import { Button } from "~/components/ui/button";
import { Input } from "~/components/ui/input";
import { Textarea } from "~/components/ui/textarea";
import { Label } from "~/components/ui/label";
import { RadioGroup, RadioGroupItem } from "~/components/ui/radio-group";

export default function NewRunPage() {
  const router = useRouter();
  const { projectId } = useActiveProject();
  const datasetId = String(router.query.id);
  const createRun = useDatasetRunCreate();

  const { data: presets } = usePresetList(projectId ?? undefined);
  const { data: prompts } = usePromptList(projectId ?? undefined);
  const { data: evalTemplates } = useEvalTemplateList(projectId ?? undefined);

  const [name, setName] = useState("");
  const [description, setDescription] = useState("");
  const [configMode, setConfigMode] = useState<"preset" | "manual">("preset");
  const [presetId, setPresetId] = useState("");
  const [promptId, setPromptId] = useState("");
  const [promptVersion, setPromptVersion] = useState(1);
  const [model, setModel] = useState("gpt-4o-mini");
  const [temperature, setTemperature] = useState(0);
  const [selectedEvals, setSelectedEvals] = useState<string[]>([]);

  const selectedPrompt = prompts?.find((p) => p.id === promptId);

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    if (!projectId) return;
    const params: Record<string, unknown> = { projectId, datasetId, name, description: description || undefined };
    if (configMode === "preset") {
      params.presetId = presetId;
    } else {
      params.model = model;
      params.modelParams = { temperature };
      if (promptId) { params.promptId = promptId; params.promptVersion = promptVersion; }
      params.evalTemplateIds = selectedEvals;
    }
    await createRun.mutateAsync(params as never);
    router.push(`/datasets/${datasetId}`);
  };

  return (
    <div className="container py-6 max-w-xl">
      <h1 className="text-2xl font-bold mb-6">New Dataset Run</h1>
      <form onSubmit={handleSubmit} className="space-y-4">
        <div>
          <Label>Run Name</Label>
          <Input value={name} onChange={(e) => setName(e.target.value)} required />
        </div>
        <div>
          <Label>Description</Label>
          <Textarea value={description} onChange={(e) => setDescription(e.target.value)} />
        </div>

        <RadioGroup value={configMode} onValueChange={(v) => setConfigMode(v as "preset" | "manual")}>
          <div className="flex items-center space-x-2">
            <RadioGroupItem value="preset" id="preset" />
            <Label htmlFor="preset">Use Preset</Label>
          </div>
          <div className="flex items-center space-x-2">
            <RadioGroupItem value="manual" id="manual" />
            <Label htmlFor="manual">Manual Configuration</Label>
          </div>
        </RadioGroup>

        {configMode === "preset" ? (
          <div>
            <Label>Preset</Label>
            <select className="w-full border rounded p-2" value={presetId} onChange={(e) => setPresetId(e.target.value)} required>
              <option value="">Select preset...</option>
              {presets?.map((p) => <option key={p.id} value={p.id}>{p.name} ({p.model})</option>)}
            </select>
          </div>
        ) : (
          <>
            <div>
              <Label>Prompt</Label>
              <select className="w-full border rounded p-2" value={promptId} onChange={(e) => { setPromptId(e.target.value); setPromptVersion(1); }}>
                <option value="">None (use raw input)</option>
                {prompts?.map((p) => <option key={p.id} value={p.id}>{p.name}</option>)}
              </select>
              {selectedPrompt && (
                <select className="w-full border rounded p-2 mt-2" value={promptVersion} onChange={(e) => setPromptVersion(Number(e.target.value))}>
                  {selectedPrompt.versions?.map((v) => <option key={v.id} value={v.version}>v{v.version}</option>)}
                </select>
              )}
            </div>
            <div>
              <Label>Model</Label>
              <Input value={model} onChange={(e) => setModel(e.target.value)} required />
            </div>
            <div>
              <Label>Temperature: {temperature}</Label>
              <Input type="range" min={0} max={2} step={0.1} value={temperature} onChange={(e) => setTemperature(Number(e.target.value))} />
            </div>
            <div>
              <Label>Eval Templates</Label>
              <div className="space-y-2 mt-2">
                {evalTemplates?.map((et) => (
                  <label key={et.id} className="flex items-center space-x-2">
                    <input type="checkbox" checked={selectedEvals.includes(et.id)} onChange={(e) => {
                      if (e.target.checked) setSelectedEvals([...selectedEvals, et.id]);
                      else setSelectedEvals(selectedEvals.filter((id) => id !== et.id));
                    }} />
                    <span className="text-sm">{et.name}</span>
                  </label>
                ))}
              </div>
            </div>
          </>
        )}

        <Button type="submit" disabled={createRun.isPending}>Execute Run</Button>
      </form>
    </div>
  );
}
```

- [ ] **Step 3: Write comparison page**

Create `web/src/pages/datasets/[id]/compare.tsx`:

```typescript
import { useState } from "react";
import { useRouter } from "next/router";
import { useActiveProject } from "~/features/projects/hooks/useActiveProject";
import { useDatasetRunCompare } from "~/features/datasets/hooks/useDatasetRuns";
import { useDatasetRunList } from "~/features/datasets/hooks/useDatasetRuns";
import { Button } from "~/components/ui/button";
import Link from "next/link";

export default function ComparePage() {
  const router = useRouter();
  const { projectId } = useActiveProject();
  const datasetId = String(router.query.id);
  const { data: runsData } = useDatasetRunList(projectId ?? undefined, datasetId);
  const [selectedRuns, setSelectedRuns] = useState<string[]>([]);

  const { data: compareData } = useDatasetRunCompare(
    projectId ?? undefined,
    datasetId,
    selectedRuns.length >= 2 ? selectedRuns : []
  );

  const toggleRun = (id: string) => {
    if (selectedRuns.includes(id)) {
      setSelectedRuns(selectedRuns.filter((r) => r !== id));
    } else if (selectedRuns.length < 5) {
      setSelectedRuns([...selectedRuns, id]);
    }
  };

  return (
    <div className="container py-6">
      <div className="flex items-center justify-between mb-4">
        <div>
          <Link href={`/datasets/${datasetId}`} className="text-sm text-muted-foreground hover:underline">← Dataset</Link>
          <h1 className="text-2xl font-bold mt-1">Compare Runs</h1>
        </div>
      </div>

      <div className="mb-4">
        <p className="text-sm text-muted-foreground mb-2">Select 2-5 runs to compare:</p>
        <div className="flex flex-wrap gap-2">
          {runsData?.data.map((run) => (
            <Button
              key={run.id}
              variant={selectedRuns.includes(run.id) ? "default" : "outline"}
              size="sm"
              onClick={() => toggleRun(run.id)}
            >
              {run.name}
            </Button>
          ))}
        </div>
      </div>

      {compareData && (
        <div className="overflow-x-auto">
          <table className="w-full text-sm border">
            <thead>
              <tr className="bg-muted">
                <th className="text-left p-2 border-r min-w-[200px]">Input</th>
                <th className="text-left p-2 border-r min-w-[150px]">Expected</th>
                {compareData.runs.map((run) => (
                  <th key={run.id} className="text-left p-2 border-r min-w-[200px]">{run.name}</th>
                ))}
              </tr>
            </thead>
            <tbody>
              {compareData.datasetItems.map((item) => (
                <tr key={item.id} className="border-t">
                  <td className="p-2 border-r align-top">
                    <pre className="text-xs bg-muted p-1 rounded overflow-auto max-h-32">{JSON.stringify(item.input, null, 2)}</pre>
                  </td>
                  <td className="p-2 border-r align-top">
                    {item.expectedOutput && (
                      <pre className="text-xs bg-muted p-1 rounded overflow-auto max-h-32">{JSON.stringify(item.expectedOutput, null, 2)}</pre>
                    )}
                  </td>
                  {compareData.runs.map((run) => {
                    const runData = (item.runs as Record<string, { traceId: string }>)?.[run.id];
                    return (
                      <td key={run.id} className="p-2 border-r align-top">
                        {runData ? (
                          <div className="text-xs">
                            <Link href={`/traces/${runData.traceId}`} className="text-blue-600 hover:underline">
                              View Trace →
                            </Link>
                          </div>
                        ) : (
                          <span className="text-xs text-muted-foreground">No data</span>
                        )}
                      </td>
                    );
                  })}
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 4: Typecheck + commit**

```bash
pnpm --filter web run typecheck
git add web/src/features/datasets/hooks/ web/src/pages/datasets/
git commit -m "feat(web): add run creation and comparison pages"
```

---

### Task 6.3: Preset pages

**Files:**

- Create: `web/src/pages/dataset-run-presets/index.tsx`
- Create: `web/src/pages/dataset-run-presets/new.tsx`

- [ ] **Step 1: Write preset list page**

Create `web/src/pages/dataset-run-presets/index.tsx`:

```typescript
import Link from "next/link";
import { useActiveProject } from "~/features/projects/hooks/useActiveProject";
import { usePresetList } from "~/features/datasets/hooks/useDatasetRunPresets";
import { Button } from "~/components/ui/button";

export default function PresetsPage() {
  const { projectId } = useActiveProject();
  const { data } = usePresetList(projectId ?? undefined);

  return (
    <div className="container py-6">
      <div className="flex items-center justify-between mb-6">
        <h1 className="text-2xl font-bold">Run Presets</h1>
        <Link href="/dataset-run-presets/new">
          <Button>+ New Preset</Button>
        </Link>
      </div>
      <table className="w-full text-sm">
        <thead>
          <tr className="border-b">
            <th className="text-left py-2">Name</th>
            <th className="text-left py-2">Model</th>
            <th className="text-left py-2">Eval Templates</th>
          </tr>
        </thead>
        <tbody>
          {data?.map((p) => (
            <tr key={p.id} className="border-b">
              <td className="py-2">{p.name}</td>
              <td className="py-2">{p.model}</td>
              <td className="py-2">{p.evalTemplateIds.length}</td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  );
}
```

- [ ] **Step 2: Write preset creation page**

Create `web/src/pages/dataset-run-presets/new.tsx`:

```typescript
import { useState } from "react";
import { useRouter } from "next/router";
import { useActiveProject } from "~/features/projects/hooks/useActiveProject";
import { usePresetCreate } from "~/features/datasets/hooks/useDatasetRunPresets";
import { usePromptList } from "~/features/prompts/hooks/usePrompts";
import { useEvalTemplateList } from "~/features/evals/hooks/useEvalTemplates";
import { Button } from "~/components/ui/button";
import { Input } from "~/components/ui/input";
import { Textarea } from "~/components/ui/textarea";
import { Label } from "~/components/ui/label";

export default function NewPresetPage() {
  const { projectId } = useActiveProject();
  const router = useRouter();
  const create = usePresetCreate();
  const { data: prompts } = usePromptList(projectId ?? undefined);
  const { data: evalTemplates } = useEvalTemplateList(projectId ?? undefined);

  const [name, setName] = useState("");
  const [description, setDescription] = useState("");
  const [promptId, setPromptId] = useState("");
  const [promptVersion, setPromptVersion] = useState(1);
  const [model, setModel] = useState("gpt-4o-mini");
  const [temperature, setTemperature] = useState(0);
  const [selectedEvals, setSelectedEvals] = useState<string[]>([]);

  const selectedPrompt = prompts?.find((p) => p.id === promptId);

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    if (!projectId) return;
    await create.mutateAsync({
      projectId,
      name,
      description: description || undefined,
      promptId: promptId || undefined,
      promptVersion,
      model,
      modelParams: { temperature },
      evalTemplateIds: selectedEvals,
    });
    router.push("/dataset-run-presets");
  };

  return (
    <div className="container py-6 max-w-xl">
      <h1 className="text-2xl font-bold mb-6">New Run Preset</h1>
      <form onSubmit={handleSubmit} className="space-y-4">
        <div><Label>Name</Label><Input value={name} onChange={(e) => setName(e.target.value)} required /></div>
        <div><Label>Description</Label><Textarea value={description} onChange={(e) => setDescription(e.target.value)} /></div>
        <div>
          <Label>Prompt</Label>
          <select className="w-full border rounded p-2" value={promptId} onChange={(e) => { setPromptId(e.target.value); setPromptVersion(1); }}>
            <option value="">None</option>
            {prompts?.map((p) => <option key={p.id} value={p.id}>{p.name}</option>)}
          </select>
          {selectedPrompt && (
            <select className="w-full border rounded p-2 mt-2" value={promptVersion} onChange={(e) => setPromptVersion(Number(e.target.value))}>
              {selectedPrompt.versions?.map((v) => <option key={v.id} value={v.version}>v{v.version}</option>)}
            </select>
          )}
        </div>
        <div><Label>Model</Label><Input value={model} onChange={(e) => setModel(e.target.value)} required /></div>
        <div><Label>Temperature: {temperature}</Label><Input type="range" min={0} max={2} step={0.1} value={temperature} onChange={(e) => setTemperature(Number(e.target.value))} /></div>
        <div>
          <Label>Eval Templates</Label>
          <div className="space-y-2 mt-2">
            {evalTemplates?.map((et) => (
              <label key={et.id} className="flex items-center space-x-2">
                <input type="checkbox" checked={selectedEvals.includes(et.id)} onChange={(e) => {
                  if (e.target.checked) setSelectedEvals([...selectedEvals, et.id]);
                  else setSelectedEvals(selectedEvals.filter((id) => id !== et.id));
                }} />
                <span className="text-sm">{et.name}</span>
              </label>
            ))}
          </div>
        </div>
        <Button type="submit" disabled={create.isPending}>Create Preset</Button>
      </form>
    </div>
  );
}
```

- [ ] **Step 3: Typecheck + commit**

```bash
pnpm --filter web run typecheck
git add web/src/pages/dataset-run-presets/
git commit -m "feat(web): add run preset list and creation pages"
```

---

## Workflow 7: SDK Updates

> Add dataset methods to TypeScript and Python SDKs.

### Task 7.1: TypeScript SDK

**Files:**

- Create: `sdk/typescript/src/datasetClient.ts`
- Modify: `sdk/typescript/src/client.ts`

- [ ] **Step 1: Write dataset client**

Create `sdk/typescript/src/datasetClient.ts`:

```typescript
import type { ConstellClient } from "./client";

export class DatasetClient {
  constructor(private client: ConstellClient) {}

  async create(params: {
    name: string;
    description?: string;
    inputSchema?: Record<string, unknown>;
    expectedOutputSchema?: Record<string, unknown>;
    metadata?: Record<string, unknown>;
  }) {
    return this.client.fetch("/datasets", { method: "POST", body: JSON.stringify(params) });
  }

  async list(params?: { page?: number; limit?: number; search?: string }) {
    const qs = new URLSearchParams();
    if (params?.page !== undefined) qs.set("page", String(params.page));
    if (params?.limit !== undefined) qs.set("limit", String(params.limit));
    if (params?.search) qs.set("search", params.search);
    return this.client.fetch(`/datasets?${qs}`);
  }

  async get(name: string) {
    return this.client.fetch(`/datasets/${name}`);
  }

  async delete(name: string) {
    return this.client.fetch(`/datasets/${name}`, { method: "DELETE" });
  }
}

export class DatasetItemClient {
  constructor(private client: ConstellClient) {}

  async create(params: {
    datasetName: string;
    input?: unknown;
    expectedOutput?: unknown;
    metadata?: Record<string, unknown>;
  }) {
    return this.client.fetch("/dataset-items", { method: "POST", body: JSON.stringify(params) });
  }

  async createMany(params: {
    datasetName: string;
    items: Array<{ input?: unknown; expectedOutput?: unknown; metadata?: Record<string, unknown> }>;
  }) {
    return this.client.fetch("/dataset-items", { method: "POST", body: JSON.stringify(params) });
  }

  async createFromTrace(params: {
    datasetName: string;
    traceId: string;
    observationId?: string;
    input?: unknown;
    expectedOutput?: unknown;
  }) {
    return this.client.fetch("/dataset-items", { method: "POST", body: JSON.stringify(params) });
  }

  async list(params: { datasetName?: string; page?: number; limit?: number }) {
    const qs = new URLSearchParams();
    if (params.datasetName) qs.set("datasetName", params.datasetName);
    if (params.page !== undefined) qs.set("page", String(params.page));
    if (params.limit !== undefined) qs.set("limit", String(params.limit));
    return this.client.fetch(`/dataset-items?${qs}`);
  }
}

export class DatasetRunClient {
  constructor(private client: ConstellClient) {}

  async create(params: {
    datasetName: string;
    runName: string;
    description?: string;
    presetName?: string;
    promptName?: string;
    promptVersion?: number;
    model?: string;
    modelParams?: Record<string, unknown>;
    evalTemplateNames?: string[];
  }) {
    return this.client.fetch("/dataset-runs", { method: "POST", body: JSON.stringify(params) });
  }

  async list(params: { datasetName: string; page?: number; limit?: number }) {
    const qs = new URLSearchParams();
    if (params.page !== undefined) qs.set("page", String(params.page));
    if (params.limit !== undefined) qs.set("limit", String(params.limit));
    return this.client.fetch(`/datasets/${params.datasetName}/runs?${qs}`);
  }

  async get(params: { datasetName: string; runName: string }) {
    return this.client.fetch(`/datasets/${params.datasetName}/runs/${params.runName}`);
  }

  async delete(params: { datasetName: string; runName: string }) {
    return this.client.fetch(`/datasets/${params.datasetName}/runs/${params.runName}`, {
      method: "DELETE",
    });
  }
}
```

- [ ] **Step 2: Wire into main client**

Modify `sdk/typescript/src/client.ts`, add properties:

```typescript
import { DatasetClient, DatasetItemClient, DatasetRunClient } from "./datasetClient";

export class ConstellClient {
  // ... existing properties
  dataset = new DatasetClient(this);
  datasetItem = new DatasetItemClient(this);
  datasetRun = new DatasetRunClient(this);
  // ...
}
```

- [ ] **Step 3: Commit**

```bash
git add sdk/typescript/src/datasetClient.ts sdk/typescript/src/client.ts
git commit -m "feat(sdk-ts): add dataset, datasetItem, datasetRun clients"
```

---

### Task 7.2: Python SDK

**Files:**

- Create: `sdk/python/src/constell/dataset_client.py`
- Modify: `sdk/python/src/constell/client.py`

- [ ] **Step 1: Write dataset client**

Create `sdk/python/src/constell/dataset_client.py`:

```python
from typing import Any, Dict, List, Optional

class DatasetClient:
    def __init__(self, client):
        self._client = client

    def create(self, name: str, description: Optional[str] = None, input_schema: Optional[Dict] = None, expected_output_schema: Optional[Dict] = None, metadata: Optional[Dict] = None):
        return self._client._request("POST", "/datasets", json={"name": name, "description": description, "inputSchema": input_schema, "expectedOutputSchema": expected_output_schema, "metadata": metadata})

    def list(self, page: int = 0, limit: int = 20, search: Optional[str] = None):
        params = {"page": page, "limit": limit}
        if search:
            params["search"] = search
        return self._client._request("GET", "/datasets", params=params)

    def get(self, name: str):
        return self._client._request("GET", f"/datasets/{name}")

    def delete(self, name: str):
        return self._client._request("DELETE", f"/datasets/{name}")

class DatasetItemClient:
    def __init__(self, client):
        self._client = client

    def create(self, dataset_name: str, input_data: Any = None, expected_output: Any = None, metadata: Optional[Dict] = None):
        return self._client._request("POST", "/dataset-items", json={"datasetName": dataset_name, "input": input_data, "expectedOutput": expected_output, "metadata": metadata})

    def create_many(self, dataset_name: str, items: List[Dict]):
        return self._client._request("POST", "/dataset-items", json={"datasetName": dataset_name, "items": items})

    def create_from_trace(self, dataset_name: str, trace_id: str, observation_id: Optional[str] = None, input_data: Any = None, expected_output: Any = None):
        return self._client._request("POST", "/dataset-items", json={"datasetName": dataset_name, "traceId": trace_id, "observationId": observation_id, "input": input_data, "expectedOutput": expected_output})

    def list(self, dataset_name: Optional[str] = None, page: int = 0, limit: int = 50):
        params = {"page": page, "limit": limit}
        if dataset_name:
            params["datasetName"] = dataset_name
        return self._client._request("GET", "/dataset-items", params=params)

class DatasetRunClient:
    def __init__(self, client):
        self._client = client

    def create(self, dataset_name: str, run_name: str, description: Optional[str] = None, preset_name: Optional[str] = None, prompt_name: Optional[str] = None, prompt_version: Optional[int] = None, model: Optional[str] = None, model_params: Optional[Dict] = None, eval_template_names: Optional[List[str]] = None):
        return self._client._request("POST", "/dataset-runs", json={"datasetName": dataset_name, "runName": run_name, "description": description, "presetName": preset_name, "promptName": prompt_name, "promptVersion": prompt_version, "model": model, "modelParams": model_params, "evalTemplateNames": eval_template_names})

    def list(self, dataset_name: str, page: int = 0, limit: int = 20):
        return self._client._request("GET", f"/datasets/{dataset_name}/runs", params={"page": page, "limit": limit})

    def get(self, dataset_name: str, run_name: str):
        return self._client._request("GET", f"/datasets/{dataset_name}/runs/{run_name}")

    def delete(self, dataset_name: str, run_name: str):
        return self._client._request("DELETE", f"/datasets/{dataset_name}/runs/{run_name}")
```

- [ ] **Step 2: Wire into main client**

Modify `sdk/python/src/constell/client.py`, add imports and properties:

```python
from .dataset_client import DatasetClient, DatasetItemClient, DatasetRunClient

class ConstellClient:
    def __init__(self, ...):
        # ... existing init
        self.dataset = DatasetClient(self)
        self.dataset_item = DatasetItemClient(self)
        self.dataset_run = DatasetRunClient(self)
```

- [ ] **Step 3: Commit**

```bash
git add sdk/python/src/constell/dataset_client.py sdk/python/src/constell/client.py
git commit -m "feat(sdk-py): add dataset, datasetItem, datasetRun clients"
```

---

## Workflow 8: Testing

> Server tests for routers and worker. E2E smoke test.

### Task 8.1: Server tests for dataset routers

**Files:**

- Create: `web/src/__tests__/server/datasets.servertest.ts`

- [ ] **Step 1: Write tests**

Create `web/src/__tests__/server/datasets.servertest.ts`:

```typescript
import { describe, it, expect, beforeAll } from "vitest";
import { appRouter } from "~/server/api/root";
import { createTRPCContext } from "~/server/api/trpc";

// Helper to create a mock context with a test project
type Context = Awaited<ReturnType<typeof createTRPCContext>>;

async function getTestContext(): Promise<Context> {
  // In real tests this would use a test user + project
  return { session: null, apiKey: { projectId: "test-project", apiKeyId: "test-key" } };
}

describe("datasets router", () => {
  let caller: ReturnType<typeof appRouter.createCaller>;

  beforeAll(async () => {
    const ctx = await getTestContext();
    caller = appRouter.createCaller(ctx);
  });

  it("creates and lists datasets", async () => {
    const created = await caller.datasets.create({
      projectId: "test-project",
      name: "Test Dataset",
    });
    expect(created.name).toBe("Test Dataset");

    const list = await caller.datasets.list({ projectId: "test-project" });
    expect(list.data.some((d) => d.id === created.id)).toBe(true);
  });

  it("rejects duplicate names", async () => {
    await caller.datasets.create({ projectId: "test-project", name: "Dup Dataset" });
    await expect(
      caller.datasets.create({ projectId: "test-project", name: "Dup Dataset" })
    ).rejects.toThrow();
  });

  it("creates items and runs", async () => {
    const ds = await caller.datasets.create({ projectId: "test-project", name: "Item Test" });

    const item = await caller.datasetItems.create({
      projectId: "test-project",
      datasetId: ds.id,
      input: { query: "hello" },
      expectedOutput: { answer: "world" },
    });
    expect(item.input).toEqual({ query: "hello" });

    const items = await caller.datasetItems.list({ projectId: "test-project", datasetId: ds.id });
    expect(items.totalCount).toBe(1);
  });
});
```

- [ ] **Step 2: Run tests**

```bash
pnpm --filter web run test -- datasets.servertest
```

Expected: tests may fail due to missing DB setup, but the test file compiles and runs.

- [ ] **Step 3: Commit**

```bash
git add web/src/__tests__/server/datasets.servertest.ts
git commit -m "test(web): add datasets router server tests"
```

---

### Task 8.2: E2E smoke test

**Files:**

- Create: `scripts/e2e-datasets.ts`

- [ ] **Step 1: Write smoke test**

Create `scripts/e2e-datasets.ts`:

```typescript
import { ConstellClient } from "../sdk/typescript/src/client";

async function smoke() {
  const client = new ConstellClient({ baseUrl: "http://localhost:3000", apiKey: "test-key" });

  console.log("1. Create dataset");
  const ds = await client.dataset.create({ name: `E2E-${Date.now()}`, description: "Smoke test" });
  console.log("  →", ds.id);

  console.log("2. Add items");
  await client.datasetItem.createMany({
    datasetName: ds.name,
    items: [
      { input: { query: "What is 2+2?" }, expectedOutput: { answer: "4" } },
      { input: { query: "What is the capital of France?" }, expectedOutput: { answer: "Paris" } },
    ],
  });

  console.log("3. Create preset");
  // Requires preset API or skip

  console.log("4. Execute run");
  const run = await client.datasetRun.create({
    datasetName: ds.name,
    runName: `run-${Date.now()}`,
    model: "gpt-4o-mini",
    modelParams: { temperature: 0 },
  });
  console.log("  →", run.id, run.status);

  console.log("5. Poll until complete");
  // Poll GET /datasets/:name/runs/:runName until status !== PENDING

  console.log("6. Compare");
  // Create second run, then compare

  console.log("✅ Smoke test passed");
}

smoke().catch(console.error);
```

- [ ] **Step 2: Commit**

```bash
git add scripts/e2e-datasets.ts
git commit -m "test(e2e): add dataset smoke test script"
```

---

## Workflow 9: Final Verification

### Task 9.1: Full verification matrix

- [ ] **Step 1: Lint and typecheck all packages**

```bash
pnpm run lint
pnpm run typecheck
```

Expected: all packages pass.

- [ ] **Step 2: Update CHANGELOG**

Append to `CHANGELOG.md`:

```markdown
## [0.9.0-beta] - 2026-05-28

### Added

- **Datasets & Experiments**
  - Dataset CRUD: name, description, input/output JSON schemas
  - DatasetItem: manual create, bulk create, create from Trace/Observation
  - DatasetRun: active LLM execution against all items
  - DatasetRunItem: links item → trace → observation
  - DatasetRunPreset: reusable execution config (prompt, model, eval templates)
  - experimentProcessor: renders prompt variables, calls LLM, ingests traces, enqueues evals
  - Run comparison UI: side-by-side matrix of items × runs
  - Public REST API: `/datasets`, `/dataset-items`, `/dataset-runs`
  - SDK: `client.dataset.*`, `client.datasetItem.*`, `client.datasetRun.*`
```

- [ ] **Step 3: Commit and tag**

```bash
git add CHANGELOG.md
git commit -m "chore(release): update CHANGELOG for v0.9.0-beta"
git tag v0.9.0-beta
git push origin main --tags
```

---

## Summary of New Files

| File                                              | Purpose                              |
| ------------------------------------------------- | ------------------------------------ |
| `packages/shared/prisma/schema.prisma`            | 5 new models                         |
| `packages/shared/src/server/llm/client.ts`        | Shared LLM client                    |
| `packages/shared/src/server/queues.ts`            | `datasetRun` queue + `DatasetRunJob` |
| `worker/src/queues/experimentProcessor.ts`        | Active execution pipeline            |
| `web/src/server/api/routers/datasets.ts`          | Dataset CRUD tRPC                    |
| `web/src/server/api/routers/datasetItems.ts`      | Item CRUD + trace-derived            |
| `web/src/server/api/routers/datasetRuns.ts`       | Run CRUD + execute + compare         |
| `web/src/server/api/routers/datasetRunPresets.ts` | Preset CRUD tRPC                     |
| `web/src/pages/datasets/**`                       | Dataset UI                           |
| `web/src/pages/dataset-run-presets/**`            | Preset UI                            |
| `sdk/typescript/src/datasetClient.ts`             | TS SDK dataset methods               |
| `sdk/python/src/constell/dataset_client.py`       | Python SDK dataset methods           |
| `web/src/__tests__/server/datasets.servertest.ts` | Server tests                         |
